#include "HeapFile.hpp"

int main(){
    // Heapfile h;
    // int page_size = 1000;
    // FILE *f = fopen("heapfile.test", "w+");
    // init_heapfile(&h, page_size, f);

    // fseekwrite(f, 0, 'c');

    // for(int i = 0; i < 20; i++){
    //     printf("%c ", fseekread(f, i));
    // }

}